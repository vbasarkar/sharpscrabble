@model Scrabble.Web.Models.PlayModel
@{ Html.RenderPartial("PlayingArea"); }
<script type="text/javascript">
    currentPlayerIndex = @(Model.HumanPlayerIndex);
    console.log('Cookie: ' + gameId());
    beginSocket();
</script>
